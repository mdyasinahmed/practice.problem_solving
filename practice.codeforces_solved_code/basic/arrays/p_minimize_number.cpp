#include<bits/stdc++.h>
using namespace std;

int main() {
    long long n;
    cin >> n;

    vector<int> arr;

    for(long long i=0; i<n; ++i) {
        cin >> arr[i];
        
        if(arr[i]%2 != 0) {
            return 0;
        } else {
            
        }
    }

    return 0;
}